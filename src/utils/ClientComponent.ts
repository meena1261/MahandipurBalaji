// import { useRouter } from "next/navigation";  // Use next/navigation in client-side

// export function ClientComponent() {
//   const router = useRouter();

//   const handleAuth = () => {
//     const tokens = "your_token_here";
//     setAuthStatus({ tokens });
//     router.push("/admin");  // This works in client-side component
//   };

//   return (
//     <button onClick= { handleAuth } > Login </button>
//   );
// }
